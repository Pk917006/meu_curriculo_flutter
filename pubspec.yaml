name: meu_curriculo_flutter
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.3
  font_awesome_flutter: ^10.12.0
  url_launcher: ^6.3.2
  provider: ^6.1.5+1
  flutter_animate: ^4.5.2
  flutter_dotenv: ^6.0.0
  supabase_flutter: ^2.10.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  logger: ^2.6.2
  import_sorter: ^4.6.0

flutter:
  uses-material-design: true
  assets:
    - assets/img/
    - assets/docs/
